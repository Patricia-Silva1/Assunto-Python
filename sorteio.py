import random 
print('========INÍCIO DA EXECUÇÃO=========')
clientes = []
nome = str(input('Digite seu nome: '))
for clientes in nome (0):
    if (clientes != 0):
        continue
    clientes += 1
input(clientes)    
telefone = []    
numero_telefone = int(input('Digite seu telefone: '))
for numero_telefone in telefone (0):
    if (numero_telefone != 0):
    input (telefone)
sorteado = random.choices(clientes, telefone)
print('Boa sorte, {}.'.format(nome))